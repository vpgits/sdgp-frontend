//write me a file uplaoader component which uses multipart/form-data to upload a file to the server and display the result

export default function UploadCard() {
    return (
        <div>
            <h1>Upload Card</h1>
        </div>
    )
}